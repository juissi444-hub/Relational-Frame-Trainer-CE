# Redirect URL for OAuth (Google, Discord, etc.)
# For production, set this to your deployed app URL (e.g., https://your-app.netlify.app)
# For local development, use http://localhost:5173 (Vite's default port)
VITE_REDIRECT_URL=http://localhost:5173

# Note: You also need to add this URL to your Supabase project's
# Authentication > URL Configuration > Redirect URLs
# Dashboard: https://supabase.com/dashboard/project/_/auth/url-configuration
